import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Subscribe to Medibot | Docrypta',
  description: 'Subscribe to Medibot for unlimited AI health queries and personalized wellness guidance.',
}

export default function SubscribeLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}